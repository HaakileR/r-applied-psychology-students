# Results Reporting Template

Use this template to write short results sections based on the R analyses.

## Descriptive statistics

The sample included N = [number] synthetic cases. The mean perceived stress score was M = [mean], SD = [standard deviation]. The mean well-being score was M = [mean], SD = [standard deviation].

## Group comparison

An independent samples t-test was conducted to compare [outcome] between [group 1] and [group 2]. The results showed [describe result], t([df]) = [t value], p = [p value].

## Correlation

A Pearson correlation was conducted to examine the association between [variable 1] and [variable 2]. The results showed [direction and strength], r = [value], p = [p value].

## Regression

A linear regression model was used to predict [outcome] from [predictors]. The model explained [R-squared]% of the variance in [outcome]. [Predictor] was a [positive/negative] predictor of [outcome], b = [coefficient], p = [p value].

## Reproducibility note

All analyses were conducted in R using scripts available in the project repository.
